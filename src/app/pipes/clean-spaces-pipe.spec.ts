import { CleanSpacesPipe } from './clean-spaces-pipe';

describe('CleanSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
