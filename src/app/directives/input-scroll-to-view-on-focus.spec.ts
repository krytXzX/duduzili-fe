import { InputScrollToViewOnFocus } from './input-scroll-to-view-on-focus';

describe('InputScrollToViewOnFocus', () => {
  it('should create an instance', () => {
    const directive = new InputScrollToViewOnFocus();
    expect(directive).toBeTruthy();
  });
});
